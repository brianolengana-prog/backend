# ==================================
# Call Sheet Converter - Backend Configuration
# ==================================
# Copy this file to .env and fill in your values

# ----------------------------------
# EMAIL SERVICE (Choose ONE provider)
# ----------------------------------

# Email Provider: gmail | smtp | sendgrid | mailgun
EMAIL_PROVIDER=gmail

# Support Contact
SUPPORT_EMAIL=support@yourdomain.com
EMAIL_FROM=noreply@yourdomain.com

# --- Gmail Configuration (Easiest for Development) ---
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# --- Generic SMTP Configuration ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password

# --- SendGrid Configuration (Recommended for Production) ---
# SENDGRID_API_KEY=SG.your-api-key-here

# --- Mailgun Configuration ---
# MAILGUN_SMTP_HOST=smtp.mailgun.org
# MAILGUN_SMTP_USER=postmaster@your-domain.mailgun.org
# MAILGUN_SMTP_PASS=your-smtp-password

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# ----------------------------------
# FEATURE FLAGS (Architecture Refactoring)
# ----------------------------------
# Enable new architecture gradually (default: false)
# Set to 'true' to enable, or use percentage for gradual rollout

# Extraction Domain
USE_NEW_EXTRACTION=false
USE_NEW_EXTRACTION_PERCENTAGE=0

# Contacts Domain
USE_NEW_CONTACTS=false
USE_NEW_CONTACTS_PERCENTAGE=0

# Auth Domain (future)
USE_NEW_AUTH=false
USE_NEW_AUTH_PERCENTAGE=0

# Billing Domain (future)
USE_NEW_BILLING=false
USE_NEW_BILLING_PERCENTAGE=0

